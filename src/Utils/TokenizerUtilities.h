#pragma once
#include <string>
#include <vector>
#include <iostream>

/**
 * A class used to traverse a string
 */
class TokenizerUtilities {
    public:
        
        static void IncrementIndex( 
            std::string text,
            size_t &m_index);
};